import sys
input = sys.stdin.readline

n = int(input())

arr = []

for i in range(n):
    
    [a, b] = map(int, input().split())
    arr.append([a,b])
    
s_arr = sorted(arr)

for j in range(n):
    print(s_arr[j][0], s_arr[j][1])
